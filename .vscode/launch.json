{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "python -m http.server 8765",
      "name": "MysticDo static server (localhost:8765)",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
